{
  "dependencies": {
    "axios": "^1.9.0",
    "cors": "^2.8.5",
    "dotenv": "^16.5.0",
    "express": "^5.1.0",
    "form-data": "^4.0.2",
    "mongoose": "^8.15.1",
    "puppeteer": "^24.9.0",
    "puppeteer-extra": "^3.3.6",
    "puppeteer-extra-plugin-adblocker": "^2.13.6",
    "puppeteer-extra-plugin-stealth": "^2.11.2",
    "user-agents": "^1.1.552"
  },
  "scripts": {
    "start": "node server/index.js",
    "dev": "concurrently \"npm start --prefix server\" \"npm run dev --prefix client\" \"cd services/job-search && node index.js\" \"cd services/resume-parser && python app.py\" \"cd services/llm-agent && python app.py\"",
    "install:all": "npm install && npm install --prefix client && npm install --prefix server && cd services/job-search && npm install && cd ../resume-parser && pip install -r requirements.txt && cd ../llm-agent && pip install -r requirements.txt"
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
